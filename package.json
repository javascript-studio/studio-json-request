{
  "name": "@studio/json-request",
  "version": "1.3.0",
  "description": "JavaScript Studio JSON Request",
  "main": "lib/request.js",
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "mocha",
    "posttest": "npm run lint",
    "watch": "mocha --watch",
    "preversion": "npm test",
    "version": "changes",
    "postversion": "git push --follow-tags && npm publish"
  },
  "author": "Maximilian Antoni <mail@maxantoni.de> (http://maxantoni.de/)",
  "eslintConfig": {
    "extends": "@studio"
  },
  "devDependencies": {
    "@studio/changes": "^1.0.2",
    "@studio/eslint-config": "^1.0.0",
    "eslint": "^3.0.1",
    "mocha": "^2.3.3",
    "sinon": "^1.17.2"
  }
}
